import { Box, Stack, Typography } from '@mui/material';

const DashboardProjectAnalytic = () => {
  return (
    <Stack sx={{ height: '100%', width: '100%', p: 2 }} justifyContent="space-between">
      <Typography variant="h6">Project Analytics</Typography>
    </Stack>
  );
};

export default DashboardProjectAnalytic;
